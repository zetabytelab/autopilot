name: Validate

on:
  push:
    branches: [main]
  pull_request:

jobs:
  readme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node scripts/validate-readme.mjs
